{-# OPTIONS --allow-unsolved-metas #-}
module Unification where

open import Syntax

--------------------------------------------------------------------------------
-- Unification (𝒰).

𝒰 : Type → Type → Subst
𝒰 τ₁ τ₂ = {!!}
